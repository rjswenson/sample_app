<% provide(:title, 'All users') %> 
<h1>All users</h1>

<%= will_paginate %>            #creates paginated links above

<ul class="users">
  <% @users.each do |user| %>
    <%= render @users %>        #Rails infers @users is User class list
  <% end %>                     #so will render partial called _user for ea
</ul>

<%= will_paginate %>            #creates paginated links below